mutation get_capabilities($data: HashObject) {
  api_call_send(
    data: $data
    template: { name: "modules/stripe/get_capabilities" }
  ) {
    response {
      status
      body
    }
    errors {
      message
    }
  }
}