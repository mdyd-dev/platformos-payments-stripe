<input type="hidden" name="{{ data_attribute }}[source]" value="" data-cc-token />
<input type="hidden" name="{{ form_builder.fields.success_message.name }}" value="You have successfully deleted an account" />

<div class="row">
  <div class="col">
    <button type="submit" class="{{ config.button_class | default: "delete-stripe btn btn-danger ml-3" }}">{{ config.button }}</button>
  </div>
</div>

